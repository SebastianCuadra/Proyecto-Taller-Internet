@model IEnumerable<clases.Models.CARRERA_RAMO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CARRERA.NOMBRE_CARRERA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RAMO.NOMBRE_RAMO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CARRERA.NOMBRE_CARRERA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RAMO.NOMBRE_RAMO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_CARRERA_RAMO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_CARRERA_RAMO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_CARRERA_RAMO })
        </td>
    </tr>
}

</table>
